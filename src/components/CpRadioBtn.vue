<script setup lang="ts">
// 校验props
const prop = defineProps<{
  option: { label: string; value: string | number }[]
  modelValue?: number | string
}>()
// 校验emit
defineEmits<{
  (name: 'update:modelValue', value: number | string): void
}>()
</script>
<template>
  <div class="cp-radio-btn">
    <a
      v-for="item in prop.option"
      :key="item.value"
      class="item"
      :class="{ active: item.value === modelValue }"
      href="javascript:;"
      @click="$emit('update:modelValue', item.value)"
      >{{ item.label }}</a
    >
  </div>
</template>
<style lang="scss" scoped>
.cp-radio-btn {
  display: flex;
  flex-wrap: wrap;
  .item {
    height: 32px;
    min-width: 60px;
    line-height: 30px;
    padding: 0 14px;
    text-align: center;
    border: 1px solid var(--cp-bg);
    background-color: var(--cp-bg);
    margin-right: 10px;
    box-sizing: border-box;
    color: var(--cp-text2);
    margin-bottom: 10px;
    border-radius: 4px;
    transition: all 0.3s;
    &.active {
      border-color: var(--cp-primary);
      background-color: var(--cp-plain);
    }
  }
}
</style>
